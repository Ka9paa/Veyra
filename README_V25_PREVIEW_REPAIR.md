# VEYRA STUDIO V25 — PREVIEW REPAIR

Main fixes:
- animated preview loading state with dots and skeleton blocks
- branded empty preview instead of a plain blank white frame
- build-stage text now appears in the preview loader
- QA panel no longer shows NaN
- QA run now shows progress before results
- refresh shows a loading state
- failed builds now say Not saved / Build failed instead of Saved
